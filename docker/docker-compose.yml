version: "3.9"

services:

  backend:
    build:
      context: ../backend
      dockerfile: ../docker/backend.Dockerfile

    container_name: mulenetx-backend

    ports:
      - "8000:8000"

    restart: unless-stopped
